version: "2"

linters:
  default: standard
  enable:
    - govet
    - staticcheck
    - errcheck
    - gofmt
    - goimports
    - ineffassign
    - unused
    - misspell
    - revive

issues:
  exclude-rules: []
